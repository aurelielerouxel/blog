{# 6 base html , extension de base.html #}
{% extends "base.html.twig" %}

{# 7 placement des block  #}
{# 9 placement block bootstrap  #}
{% block body %}
    <header class="jumbotron jumbotron-fluid bg-dark text-white mb-0">
        <section class="container">
            <h1 class="display-4">Forum Drive</h1>
            <p class="lead">Parler de tout et n'importe quoi</p>
        </section>
    </header>

{# 14 installation nav  #}
    <nav>
        <ul class="nav mt-0 bg-danger">
            <li class="nav-item">
{# 15 fonction twig pour rediriger l'utilisateur vers des routes  #}
                <a class="nav-link text-white" href="{{ path("index") }}">Acceuil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="{{ path("rules") }}">Règles</a>
            </li>
        </ul>
    </nav>

{# 10 appel bloc pour index & rules  #}
    <main class="container my-5">
    {% block main %}{% endblock %}
    </main>

    <footer class="jumbotron jumbotron-fluid bg-dark text-white">
        <section class="container">
            <p class="lead">Tous droits réservés</p>
        </section>
    </footer>
{% endblock %}